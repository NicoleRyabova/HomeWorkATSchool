package Lecture6_4.Task1;

public class scatch {
    /*else {
            if (!(args[0].equalsIgnoreCase("Half") || args[0].equalsIgnoreCase("Double") || args[0].equalsIgnoreCase("Exact") || args[0].equalsIgnoreCase("Square"))) {
                System.out.printf("Операция %s не поддерживается", args[0]);
            } else {
                if (args[1].isEmpty()) {
                    System.out.println("Не переданы числа для операции");
                } else {
                    int[] arrayFilledWithNumbersFromArgs = new int[args.length];
                    for (int i = 1; i < args.length; i++) {
                        arrayFilledWithNumbersFromArgs[i] = Integer.parseInt(args[i]); //заполняю массив с числами из args
                    }
                    if(args[0].equalsIgnoreCase("Half")){
                        applyFunction(arrayFilledWithNumbersFromArgs, half);

                    }
                }
            }
        }*/
}
